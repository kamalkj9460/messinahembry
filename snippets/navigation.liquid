{% capture link_handle %}{{ link.title | handle }}{% endcapture %}
{% capture linkurl %}{{ link.url }}{% endcapture %} 
{% capture child_list_handle %}{{ link.title | handleize }}{% endcapture %}

{% if link.title == settings.megadropdown_link %}
<li class="dropdown">{{ link.title | link_to: link.url }}  

  <ul class="test megamenu {% if settings.mega_4_show %} desktop-9 {% elsif settings.mega_3_show %}desktop-7{% else %}desktop-5{% endif %} tablet-6">
    <div class="row">

    <span class="nav-container {% if settings.nav_promo_image %}{% if settings.mega_3_show or settings.mega_4_show %}desktop-9{% else %}desktop-7{% endif %}{% else %} desktop-12{% endif %} tablet-6">
      {% assign mega = 0 %}
      {% if settings.mega_1_show %}{% assign mega = mega | plus: 1 %}{% endif %}
      {% if settings.mega_2_show %}{% assign mega = mega | plus: 1 %}{% endif %}
      {% if settings.mega_3_show %}{% assign mega = mega | plus: 1 %}{% endif %}
      {% if settings.mega_4_show %}{% assign mega = mega | plus: 1 %}{% endif %}

      {% if mega > 0 %}    
      <!-- Start Megamenu Inner Links -->
      {% if settings.mega_1_show %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_1_links].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_1_links].links %}
          <li>            {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}
          </li>
          {% endfor %}
        </ul>
      </li> 
      {% endif %}  
      {% if settings.mega_2_show %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_2_links].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_2_links].links %}
          <li>            {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}
          </li>
          {% endfor %}
        </ul>
      </li> 
    
      {% endif %}
      {% if settings.mega_3_show %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_3_links].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_3_links].links%}
          <li>            {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}</li>
          {% endfor %}
        </ul>
      </li>
      {% endif %}
      {% if settings.mega_4_show %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_4_links].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_4_links].links %}
          <li>            {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}</li>
          {% endfor %}
        </ul>
      </li> 
      {% endif %}    
      {% endif %}  
    </span>


    </div>
  </ul>
</li>

{% elsif link.title == settings.megadropdown_link2 %}
<li class="dropdown">{{ link.title | link_to: link.url }}  

  <ul class="megamenu {% if settings.mega_4_show2 %} desktop-9 {% elsif settings.mega_3_show2 %}desktop-7{% else %}desktop-5{% endif %} tablet-6">
    <div class="row">

    <span class="nav-container {% if settings.mega_3_show4 %} desktop-6 {% elsif settings.mega_4_show3 %}desktop-9{% else %}desktop-12{% endif %} tablet-6">
      {% assign mega = 0 %}
      {% if settings.mega_1_show2 %}{% assign mega = mega | plus: 1 %}{% endif %}
      {% if settings.mega_2_show2 %}{% assign mega = mega | plus: 1 %}{% endif %}
      {% if settings.mega_3_show2 %}{% assign mega = mega | plus: 1 %}{% endif %}
      {% if settings.mega_4_show2 %}{% assign mega = mega | plus: 1 %}{% endif %}

      {% if mega > 0 %}    
      <!-- Start Megamenu Inner Links -->
      {% if settings.mega_1_show2 %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_1_link2].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_1_link2].links %}
          <li>             {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}</li>
          {% endfor %}
        </ul>
      </li> 
      {% endif %}  
      {% if settings.mega_2_show2 %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_2_link2].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_2_link2].links %}
          <li class="{% if link.url == '#heading' %}headed{% else %}null {% endif %}">
            {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}
          </li>
          {% endfor %}
        </ul>
      </li> 
    
      {% endif %}
      {% if settings.mega_3_show2 %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_3_link2].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_3_link2].links%}
          <li>            {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}</li>
          {% endfor %}
        </ul>
      </li>
      {% endif %}
      {% if settings.mega_4_show2 %}            
      <li class="desktop-{{ 12 | divided_by: mega }} tablet-fourth"><h4>{{ linklists[settings.mega_4_link2].title }}</h4>
        <ul class="mega-stack">
          {% for link in linklists[settings.mega_4_link2].links %}
          <li> {% if link.url == '#heading' %}
            <h6 class="heading">{{ link.title }}</h6>
            {% else %}
            {{ link.title | link_to: link.url }}
            {% endif %}</li>
          {% endfor %}
        </ul>
      </li> 
      {% endif %}    
      {% endif %}  
    </span>


    </div>
  </ul>
</li>

{% else %}
<li class="standard-link"><a class="top-level" href="{{ link.url }}">{{ link.title }}</a></li>
{% endif %}